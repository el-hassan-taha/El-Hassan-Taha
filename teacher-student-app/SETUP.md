# دليل الإعداد والتشغيل - Setup Guide

## المتطلبات الأساسية

- Node.js 18 أو أحدث
- حساب Supabase مجاني
- محرر نصوص (VS Code موصى به)

## خطوات الإعداد التفصيلية

### 1. إعداد Supabase

#### إنشاء المشروع
1. اذهب إلى [Supabase](https://supabase.com)
2. قم بإنشاء حساب جديد أو سجل الدخول
3. انقر على "New Project"
4. اختر اسم المشروع وكلمة مرور قوية لقاعدة البيانات
5. اختر المنطقة الأقرب إليك (مثل: Europe West)
6. انقر على "Create new project"

#### الحصول على مفاتيح API
1. بعد إنشاء المشروع، اذهب إلى Settings (الإعدادات) > API
2. ستجد:
   - `Project URL` (عنوان المشروع)
   - `anon/public key` (المفتاح العام)
3. احتفظ بهذه المعلومات لاستخدامها لاحقاً

#### إنشاء الجداول
1. اذهب إلى SQL Editor
2. افتح ملف `supabase-schema.sql` من المشروع
3. انسخ المحتوى بالكامل
4. الصقه في SQL Editor
5. انقر على "Run" لتنفيذ الاستعلام
6. تأكد من ظهور رسالة نجاح

#### إعداد المصادقة
1. اذهب إلى Authentication > Users
2. انقر على "Add user" > "Create new user"
3. أدخل بريد إلكتروني وكلمة مرور للمعلم
4. انقر على "Create user"

### 2. إعداد المشروع

#### تثبيت المكتبات
```bash
cd teacher-student-app
npm install
```

#### إعداد متغيرات البيئة
```bash
cp .env.example .env.local
```

افتح `.env.local` وأدخل معلومات Supabase:
```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
```

### 3. تشغيل المشروع

#### وضع التطوير
```bash
npm run dev
```

سيعمل المشروع على: http://localhost:3000

#### البناء للإنتاج
```bash
npm run build
npm start
```

## تسجيل الدخول

### المعلم
- استخدم البريد الإلكتروني وكلمة المرور التي أنشأتها في Supabase
- صفحة تسجيل الدخول: `/`
- بعد تسجيل الدخول: `/teacher/dashboard`

### الطالب
- استخدم الرقم القومي للطالب
- يجب أن يكون الطالب مسجلاً في قاعدة البيانات أولاً
- بعد تسجيل الدخول: `/student/home`

## إضافة بيانات تجريبية

### إضافة طالب
1. سجل دخول كمعلم
2. اذهب إلى "إدارة الطلاب"
3. انقر على "إضافة طالب جديد"
4. املأ البيانات:
   - الاسم الكامل: محمد أحمد
   - الرقم القومي: 12345678901234
   - السنة الدراسية: 2024-2025
   - الصف: الأول
   - الفصل: أ
   - رقم هاتف ولي الأمر: 01234567890

### إضافة مهمة
1. اذهب إلى "إدارة المهام"
2. انقر على "إضافة مهمة جديدة"
3. املأ البيانات:
   - عنوان المهمة: حل تمارين الرياضيات
   - الوصف: حل التمارين من صفحة 10 إلى 15
   - تاريخ التسليم: اختر تاريخ
   - السنة الدراسية: 2024-2025
   - الصف: الأول
   - الفصل: أ

سيتم تعيين المهمة تلقائياً لجميع الطلاب في هذا الصف والفصل.

## حل المشاكل الشائعة

### خطأ في الاتصال بـ Supabase
- تأكد من صحة `NEXT_PUBLIC_SUPABASE_URL` و `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- تأكد من عدم وجود مسافات زائدة
- تأكد من أن المفاتيح تبدأ بـ `NEXT_PUBLIC_`

### لا يمكن تسجيل الدخول كمعلم
- تأكد من إنشاء المستخدم في Supabase Authentication
- تأكد من صحة البريد الإلكتروني وكلمة المرور
- تحقق من Console في المتصفح (F12) لرؤية الأخطاء

### لا يمكن تسجيل الدخول كطالب
- تأكد من إضافة الطالب أولاً عبر واجهة المعلم
- تأكد من صحة الرقم القومي (14 رقم بدون مسافات)

### خطأ في بناء المشروع
```bash
# امسح cache وأعد التثبيت
rm -rf .next node_modules
npm install
npm run build
```

## الميزات المتاحة حالياً

✅ تسجيل الدخول (معلم وطالب)
✅ لوحة تحكم المعلم مع إحصائيات
✅ إدارة الطلاب (إضافة، بحث، تصفية)
✅ إدارة المهام (إضافة، تعيين تلقائي)
✅ صفحة الطالب الرئيسية مع مقاييس الأداء
✅ صفحة تفاصيل الطالب

## الميزات قيد التطوير

🚧 إدارة الحضور الأسبوعي
🚧 إدارة الامتحانات والدرجات
🚧 تحديث حالة المهام للطلاب
🚧 طباعة تقارير PDF
🚧 الحذف الجماعي للطلاب
🚧 الإشعارات

## الدعم

للمساعدة والأسئلة:
- افتح Issue في GitHub
- راجع الوثائق الرسمية:
  - [Next.js Documentation](https://nextjs.org/docs)
  - [Supabase Documentation](https://supabase.com/docs)
  - [TailwindCSS Documentation](https://tailwindcss.com/docs)
